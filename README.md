# Mini-Helfer — Landing + How it works
- Two routes: `/` (landing) and `/how-it-works`
- Next.js 14 + Tailwind + lucide-react
- Static export enabled

## Run
npm i
npm run dev

## Static export setup
npm run export
# output: ./out
